import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme)=>({
    root: {
        maxWidth: 300,
        height: 'auto',
        marginBottom: 50
    },
    media: {
        height: 'auto',
        width: '100%'
    },
    cardHeader: {
        textAlign: 'center'
    }
}));