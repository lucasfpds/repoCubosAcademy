import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme)=>({
    input: {
        width: '100%',
        maxWidth: 300,
    },
    btn: {
        backgroundColor: 'rgb(255, 0, 98)!important',
        width: '100%',
        maxWidth: 300,
    }
}));